@using MvcLibraryApp.Models.Entities;
@model List<Employee>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
<link href="path/to/bootstrap-icons.css" rel="stylesheet">

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h3 class="text-center mb-4" style="color: #e74c3c; font-size: 2rem;">Employee List</h3>
            <a href="/Employees/Add" class="btn btn-primary mb-3" style="background-color: #3498db; border-color: #2980b9;">Add Employee</a>
            <table class="table table-striped" style="margin-top:10px;">
                <thead>
                    <tr>
                        <th scope="col">Id</th>
                        <th scope="col">Name</th>
                        <th scope="col">LastName</th>
                        <th scope="col">Age</th>
                        <th scope="col">Delete Update</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var employee in Model)
                    {
                        <tr>
                            <th scope="row">@employee.Id</th>
                            <td>@employee.Name</td>
                            <td>@employee.LastName</td>
                            <td>@employee.Age</td>
                            <td>
                                <a href="/Employees/Delete/@employee.Id" class="btn btn-danger"><i class="bi bi-trash-fill"></i></a>
                                <a href="/Employees/Update/@employee.Id" class="btn btn-warning"><i class="bi bi-pencil-fill"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
</table>